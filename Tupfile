: |> clang -shared support/macros.c -o %o |> libshcl-support.so
ifdef CONFIG_BUILD_RULES_FILE
include @(BUILD_RULES_FILE)
else
: |> @(LISP_LOAD) ./make.lisp |> shcl
endif

ifdef CONFIG_RUN_TESTS
: | shcl |> TEST_OUTPUT="%o" @(LISP_LOAD) ./test.lisp > /dev/null |> test-output.txt
endif

.gitignore
